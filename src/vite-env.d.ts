/// <reference types="vite/client" />

/** Injected by vite.config.ts at build time — see the note there. In `vite dev`
 *  it is the moment the dev server started, which is near enough "just now". */
declare const __BUILD_TIME__: string
